# Active work

- Packaging and validation scripts now clear `artifacts/` before building; manual builds must use the same cleanup script. When requested, check repeated packaging and cleanup with synthetic output, hidden files and links/junctions. No execution checks were run for this change.

- September 15 source redesigns the whole wizard: simplified sidebar and fixed headings, one Welcome introduction, centered Add account and account-sharing dialogs, compact account summaries, minimal Codex status with diagnostics in a dialog, and Search/Usage/Limits settings tabs. No build, tests or desktop checks were run. When requested, exercise all four demo screens at narrow/high-DPI sizes, pointer exit/reactivation and keyboard/touch scrolling; repeat dialog open/close, save/discard, calendar selection handoff/cancel, provider-setup return, local progress/errors and restart notices before packaging. The installed application does not reflect these source changes yet.

- September 14 source adds Inbox-only search through the application and providers, enabled by default for MCP unread search and optional for general/date-range search. Provider-side scoping avoids hydrating archived or moved messages; results report the scope and cursors retain it. This increment is not built, tested or installed. When requested, check Inbox/date/unread combinations, all Inbox categories, custom labels, archived/moved messages, broader-scope opt-out and cursor scope changes using synthetic accounts, then the published executable. Earlier package evidence does not validate this increment.

- September 13 source adds the Windows read-budget editor and aggregate usage snapshots through the application facade, with atomic/stale-aware saves, restart guidance, protected drafts and isolated demo state. MCP status reports aggregate usage without consuming budgets. These changes are not built, tested or installed. When requested, run the added synthetic cases and exercise native input validation, save/discard, concurrent saves, cancellation, narrow layouts and post-restart settings in isolated profiles. OAuth refresh backoff, weighted quota accounting, Graph completeness/ordering and model-token integration remain separate follow-ups.

- The September 12 API/output guardrail source increment adds shared request/cooldown ledgers, Microsoft 429 recovery, cumulative detail/output budgets, smaller adaptive mail pages, memory detail caches, Microsoft folder caching and Gmail MIME fixes. It is not built, tested or installed; `0.1.1.20` remains the earlier UI package. When requested, exercise the new synthetic suites and a published executable with isolated profiles. Follow-up: Windows budget controls/usage display, OAuth refresh failure backoff, weighted quota accounting, Graph search completeness/ordering and client-side token metering. See [guardrails](../../docs/READ_GUARDRAILS.md).

- Windows x64 MSIX `0.1.1.20` is built, signed and locally installed (`Ok`, valid signature). It includes CLI screen selection, isolated UI demo and Welcome, Accounts, Sharing and Codex layout refinements. The earlier local preview passed 291 synthetic tests and CLI/MCP smoke. Native screen rendering, existing-window step changes, unsaved-edit navigation and demo save/discard interactions remain to be reviewed. This increment has not been added to the earlier mail-search PR.
- Windows x64 MSIX `0.1.1.19` is built, signed and locally installed (`Ok`, valid signature), superseding `0.1.1.18`. It includes the configurable 14-day default, effective-window reporting, Microsoft search date bounds, Google rate-limit recovery and the Windows Sharing period editor. A subsequent owner-requested run passed all 229 .NET tests (zero failures/skips) and isolated CLI/MCP smoke against the installed executable. Desktop save/discard/rendering and real-provider compatibility remain pending. Historical package/source records below describe earlier states.
- After the `0.1.1.18` upgrade, owner-requested plugin mail reads on September 7–8 completed across all five shared accounts without reported failures, including unread search, pagination, individual-account empty results and selected detail reads. Preserve this evidence separately from the still-pending synthetic regressions, desktop interactions and calendar checks. See [validation](../../docs/VALIDATION.md).
- Windows x64 MSIX `0.1.1.18` is now built, signed and installed (`Ok`, valid signature), superseding `0.1.1.17`. It includes the Gmail HTTP 204/calendar sampling fixes and the actionable Codex screen below. Regression tests were not compiled or run by this packaging request; when explicitly requested, run the synthetic tests and validate the updated UI and real-account scenarios. Earlier source-only and package records below are historical, not the current installed state.
- The Connect to Codex screen now reports individual configuration findings and actionable guidance, including marketplace-added versus plugin-installed and direct/plugin coexistence. CLI prompt execution remains deferred. Source and synthetic parser/presentation regressions are not built or executed; when requested, validate refresh/cancellation, partial command failures, marketplace-only setup, direct registrations, plugin enablement, clipboard feedback and narrow/high-DPI layout before packaging. Do not mutate existing Codex connections during a check.
- The source fix for Gmail message-list HTTP 204 responses and the five-calendar check cutoff is implemented with synthetic regressions, but has not been compiled, tested or packaged. When requested, run the diagnostic/provider and read-check tests, build/install the update, then verify the original empty-search and multi-calendar scenarios. Required details, malformed responses, deadlines and incomplete pagination must not become false successes.
- Windows x64 MSIX `0.1.1.17` is the last signed and installed package, with correlated logs and minimal account rows; it does not contain the follow-up fix above. Its build/packaging passed; regression tests compiled but were not executed. Owner-triggered `0.1.1.16` logs showed Google HTTP 204 JSON parsing failures and a local calendar sampling limit. Microsoft sample mail reads passed; absent event samples did not indicate sign-in failure. See [validation](../../docs/VALIDATION.md).
- Windows x64 MSIX `0.1.1.15` is signed and installed with account recovery, larger package icons and the Microsoft text-search fix. Local validation passed 122 .NET tests and native CLI/MCP smoke. The installed executable passed 34 real-provider checks with zero failures and three calendar skips across five accounts; both Microsoft accounts passed search, detail and continuation. Standalone and installed runtimes exposed different default account sets, so keep their validation evidence separate. See [validation](../../docs/VALIDATION.md).
- The approved Acrylic wizard and desktop single-instance activation are included in locally installed Windows x64 MSIX `0.1.1.10`, with local artwork/provider SVGs, unnumbered progress, account pagination, a selected-account sharing editor and an explicit **Check connections** action. The action checks every local account with minimal Mail/Calendar provider requests, silently refreshes OAuth/MSAL tokens where possible and reports only safe reconnect guidance. The updates fix vector logos loaded as bitmap sources, a custom entry point that left the process running without starting the setup window, and an unavailable optional high-contrast event. Desktop Acrylic replaces dark Mica to make the enabled system transparency visible. Release build, 113 shared .NET tests, package signing/upgrade, the installed alias CLI/MCP smoke and a synthetic native-window launch without diagnostics passed.
- The owner requested to review rendering personally, without Computer Use. Desktop startup, duplicate launches (including a minimized window), adaptive layout, search/pagination, calendar selection, unsaved choices and existing sharing restrictions remain untested for this redesign. Use synthetic accounts and an isolated data directory for future automated checks.
- Earlier `0.1.1.4` synthetic existing-registry startup and earlier About/banner inspection do not validate the redesigned UI. Validate clean-machine deployment, additional DPI settings, Google/Microsoft UI sign-in and local Codex plugin installation when requested.
- Exercise simultaneous installed-alias callers and an MSIX upgrade without changing the Codex command; redirected MCP stdio already passed on the installed alias.
- Synthetic sharing and caller-notification regressions passed, including cached references and in-flight reads. Validate the corresponding live account scenarios only when requested.
- Preserve the package identity and signing publisher across upgrades; do not publish or install on the owner's machine without a request.

- The earlier Windows CLI source passed 68 .NET tests, 24 manual-runner regressions and 27 real-provider checks; three event checks were skipped. These historical live checks are separate from the current synthetic/UI validation. See [validation](../../docs/VALIDATION.md).
- Windows x64 package `de1fae7` passed native and extracted smoke checks, live reads and preservation of all four accounts. Clean installation remains.
- Compare known mail and calendar examples independently, including recurrence, cancellation, all-day dates and time zones.
- Deliberately check real expiry, revoked access, reconnect and lost connectivity when requested. Existing synthetic fault tests do not validate those live paths.
- Check installation on a clean Windows machine and start a small pilot. Windows ARM64 is build-only; macOS/Linux runtime testing is outside the current MVP.
- Keep email and calendar access strictly read-only. Provider writes need a separate explicit decision.
- Validate the new unread/date-range mail tools, structured filters and Spam/Junk plus Trash/Deleted exclusions against synthetic and real provider cases.
- Maintain the progress plan in the versioned [docs/MVP_PLAN.md](../../docs/MVP_PLAN.md); do not maintain separate output copies.
